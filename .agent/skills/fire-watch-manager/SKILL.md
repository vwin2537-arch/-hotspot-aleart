---
name: fire-watch-manager
description: "คู่มือการจัดการจุดเฝ้าระวังและมาตรฐาน UI สำหรับโปรเจครายงานไฟป่า"
---

# 🔥 Fire Watch Manager Skill

สกิลนี้ใช้สำหรับควบคุมมาตรฐานการแก้ไขโค้ดในโปรเจค Fire Checkpoint Report เพื่อให้ระบบทำงานถูกต้องและสวยงามตามที่ USER ต้องการ

## 1. มาตรฐานการจัดการจุดเฝ้าระวัง (Watch Points)
- **ไฟล์ที่เกี่ยวข้อง:** [constants.ts](file:///c:/Users/This%20PC/OneDrive/01_FireControl/000_Ai%20Project/FireCheckPointReport/constants.ts)
- **กฎการตั้งชื่อ (ID & Name):** 
  - ต้องใช้ฟังก์ชัน `formatPointName` (ใน `App.tsx`) เพื่อเติมเลข 0 ข้างหน้าให้เป็น 2 หลักเสมอ เช่น "จุดเฝ้าระวังที่ 01"
  - หากมีการเพิ่มจุดใหม่ ต้องอัปเดตทั้งใน Frontend (`WATCH_POINTS`) และแจ้ง USER ให้อัปเดต `TOTAL_WATCH_POINTS` ใน Backend (GAS) ด้วย
- **จุดที่ต้องระวัง:** จำนวนจุดผลรวม (Total Goal) ในหน้า Dashboard คือ `จำนวนจุด x 3 (เช้า, บ่าย, เย็น)`

## 2. มาตรฐานด้านเวลาและวันที่ (Date & Time)
- **การดึงวันที่:** ห้ามใช้ `new Date().toISOString()` ตรงๆ สำหรับการแสดงผลหรือบันทึกข้อมูล
- **ฟังก์ชันมาตรฐาน:** ต้องใช้ `getToday()` ที่แยก Year-Month-Day เอง เพื่อป้องกันปัญหา Timezone (UTC+7)
- **ตัวอย่าง:**
  ```javascript
  const getToday = () => {
    const d = new Date();
    const year = d.getFullYear();
    const month = String(d.getMonth() + 1).padStart(2, '0');
    const day = String(d.getDate()).padStart(2, '0');
    return `${year}-${month}-${day}`;
  };
  ```

## 3. มาตรฐานความสวยงาม (UI/UX Standards)
- **Theme:** ใช้ระบบ Dark Mode เป็นหลัก (Slate-900 / Emerald-600)
- **หน้า Dashboard (Arcade Style):**
  - เน้นความ "ว้าว" ด้วย Gradient และ Glow Effects
  - ใช้ `Recharts` สำหรับการแสดงกราฟ
  - ปุ่มกดต้องมี Micro-animations (เช่น `active:scale-95`)
- **การจัดการรูปภาพ:** 
  - ต้องมีการ Compress รูปภาพก่อนส่งเสมอ (ผ่าน `utils/imageCompression.ts`)
  - จำกัดไม่เกิน 5 รูปต่อจุด

## 4. มาตรฐานการส่งมอบงานและการสื่อสาร (Delivery & Communication)
- **การส่งโค้ด Backend (Google Apps Script):** **ห้าม** ส่งแค่โค้ดบางส่วน (Snippets) ให้ส่ง **โค้ดเต็มไฟล์** เสมอ เพื่อให้ USER ก๊อปปี้ไปวางทับใน Google Apps Script Editor ได้ทันทีโดยไม่ต้องไล่หาบรรทัด
- **ภาษาที่ใช้:** อธิบายด้วยภาษาที่เข้าใจง่าย หลีกเลี่ยงศัพท์เทคนิคที่ซับซ้อนเกินไป หากต้องใช้คำศัพท์เฉพาะ ให้เปรียบเทียบกับสิ่งที่เห็นจริงในหน้าจอ

## 5. ความสอดคล้องของระบบ (System Synchronization)
- **กฎเหล็กเรื่อง Backend Sync:** ทุกครั้งที่มีการแก้ไข Frontend ที่ส่งผลต่อข้อมูล (เช่น เพิ่มจุดตรวจ, เปลี่ยนชื่อกลุ่ม, เปลี่ยนหัวข้อรายงาน) **ต้อง** แจ้งเตือน USER ให้ไปอัปเดตโค้ดในไฟล์ `line-bot-notification.js` ฝั่ง Backend ด้วยเสมอ
- **การตรวจสอบผลกระทบ:** ก่อนตอบงาน ให้ถามตัวเองเสมอว่า "การแก้นี้ต้องไปแก้ที่ Google Script ด้วยไหม?" ถ้าใช่ ให้เตรียมโค้ดฝั่งนั้นให้ USER ทันที

## 6. ความปลอดภัยและการเข้าถึง (Security & Access)
- **รหัสผ่าน Admin:** รหัสผ่านมาตรฐานคือ `2518` **ห้าม** เปลี่ยนแปลงหรือแก้ไขในโค้ดโดยไม่ได้รับคำสั่งจาก USER
- **การจัดการ Token:** หากมีการขอให้นำโค้ดไปเผยแพร่หรือสำรองข้อมูล (เช่น ขึ้น GitHub หรือส่งผ่านแชท) **ต้อง** เตือน USER ให้ลบ `CHANNEL_ACCESS_TOKEN` และ `GROUP_ID` ออกก่อนเสมอเพื่อความปลอดภัย

## 7. บันทึกการแก้ปัญหา (Troubleshooting Memory)
- **Dashboard Data Delay:** ข้อมูลหน้า Dashboard มีการทำ Cache ไว้ 10 นาที (ฝั่ง Backend) หาก USER แจ้งว่าข้อมูลไม่อัปเดต ให้แนะนำวิธีล้าง Cache หรือรอตามเวลา
- **Timezone Fix:** ห้ามกลับไปใช้ระบบวันที่มาตรฐานของ JS (ISO String) เพราะจะทำให้เวลาไทยเพี้ยนในตอนเช้ามืด ต้องใช้ `getToday()` ที่กำหนดไว้ในส่วนที่ 2 เท่านั้น
- **Image Size:** หากระบบค้างตอนส่งรายงาน ให้เช็คว่ามีการส่งรูปเกิน 5 รูป หรือรูปมีขนาดใหญ่เกินไปหรือไม่ (ระบบต้องบีบอัดรูปก่อนส่งเสมอ)

## 8. การรักษาข้อมูลออฟไลน์ (Offline Data Integrity)
- **LocalStorage:** ระบบมีการเก็บข้อมูลรายงานที่ยังส่งไม่สำเร็จลงใน `pending_reports`
- **กฎการแก้ไข:** ห้ามแก้ไขชื่อตัวแปรหรือโครงสร้างข้อมูลในส่วนของ Offline Mode ลอยๆ เพราะจะทำให้ข้อมูลที่ USER บันทึกค้างไว้ในเครื่อง (ขณะไม่มีเน็ต) สูญหายหรือส่งไม่ผ่านเมื่อเน็ตกลับมา

## 9. มาตรฐานโค้ดสะอาดและขยายง่าย (Modular Code)
- **Component Separation:** หาก UI ส่วนไหนเริ่มยาวเกิน 100 บรรทัด หรือมีการใช้ซ้ำ (เช่น ปุ่ม, การ์ดแสดงผล) **ต้อง** แยกออกมาเป็นไฟล์ใหม่ในโฟลเดอร์ `components/` เสมอ
- **DRY (Don't Repeat Yourself):** หลีกเลี่ยงการเขียน Logic ซ้ำซ้อน หากมีฟังก์ชันที่ใช้ทั้งหน้า Form และ Dashboard ให้ย้ายไปไว้ใน `utils/` หรือ `services/`

## 10. การจัดทำเอกสารสำหรับผู้ใช้งาน (Developer-to-User Documentation)
- **Thai Comments:** ในฟังก์ชันที่ซับซ้อน (โดยเฉพาะหน้า Backend GAS) **ต้อง** ใส่ Comment ภาษาไทยอธิบาย "เหตุผล" และ "หน้าที่" ของบรรทัดนั้นๆ เพื่อให้ USER (ที่ไม่ได้จบสายไอที) เข้าใจที่มาที่ไปได้
- **ตัวอย่าง:** `// ตรวจสอบว่ามีข้อมูลซ้ำไหม เพื่อป้องกันการส่งรายงานซ้อนกัน`

## 11. มาตรฐานการออกแบบเพื่อใช้งานในพื้นที่จริง (Field-Ready UI/UX)
- **Mobile-First Check:** เจ้าหน้าที่ต้องใช้งานในป่าที่มีแสงจ้าหรือใส่ถุงมือ:
  - ปุ่มกดต้องมีขนาดใหญ่พอ (อย่างน้อย 44x44 pixels)
  - ระยะห่างระหว่างปุ่มต้องมีเพียงพอ (Gap) ไม่ให้กดพลาด
  - สีพื้นหลังและสีตัวอักษรต้องมีค่า Contrast สูงทำให้อ่านง่ายกลางที่แจ้ง
  - ต้องมีสถานะ Loading (ตัวหมุน) ทุกครั้งที่กดส่ง เพื่อให้ USER รู้ว่าระบบกำลังทำงาน ไม่กดปุ่มซ้ำ

## 12. มาตรฐานการแสดงข้อมูลด้วยภาพ (Data Visualization Standards)
- **การเลือกใช้กราฟ:** 
  - ใช้ **Bar Chart** สำหรับเปรียบเทียบจำนวนการส่งรายงานแต่ละจุด
  - ใช้ **Pie Chart** สำหรับดูสัดส่วนการปฏิบัติงาน (สำเร็จ vs คงค้าง)
  - ใช้ **Area/Line Chart** หากต้องการแสดงแนวโน้ม (Trends) ของเหตุไฟป่าตามช่วงเวลา
- **ความสามารถของกราฟ:**
  - ต้องมี **Tooltip** ที่แสดงข้อมูลภาษาไทยชัดเจนเมื่อ User นำเมาส์ไปชี้ (หรือแตะบนมือถือ)
  - ต้องใช้ **ResponsiveContainer** เพื่อให้กราฟปรับขนาดตามหน้าจออัตโนมัติ
  - ใช้โทนสีที่สื่อความหมายชัดเจน (เขียว = สำเร็จ, แดง = แจ้งเหตุไฟ, เทา = ยังไม่ดำเนินการ)

## 13. มาตรฐานระบบแจ้งเตือนอัจฉริยะ (Smart Notification Logic)
- **Proactive Alerts:** ระบบควรมีการเช็ค Conditional แจ้งเตือนเอง (เช่น แจ้งเตือนผ่าน LINE เมื่อมีเหตุไฟป่าใหม่ หรือเมื่อใกล้หมดเวลาเวรแต่ยังส่งรายงานไม่ครบ)
- **Rich Interaction:** การแจ้งเตือนใน LINE Messaging API ควรประกอบด้วย:
  - **Text + Image:** แจ้งเหตุพร้อมภาพประกอบเพื่อให้เห็นสถานการณ์จริง
  - **Quick Reply/Buttons:** มีปุ่มทางลัด เช่น "รับทราบเหตุ", "ดูพิกัด", "ดู Dashboard" เพื่อลดขั้นตอนการทำงานของเจ้าหน้าที่
- **Context Awareness:** ระบบแจ้งเตือนต้องสามารถระบุชื่อจุดและช่วงเวลาที่เกิดปัญหาได้แม่นยำ ไม่แจ้งเตือนแบบกว้างๆ จนสร้างความสับสน

## 14. มาตรฐานโครงสร้าง Modal และ Timeline (Modal & Timeline Hierarchy Standards)
สกิลนี้ช่วยแก้ปัญหาหน้าจอเบี้ยว ทับซ้อน และการเลื่อนที่ไม่เป็นระบบ:

- **การจัดกึ่งกลาง (Centering Strategy):**
  - ต้องใช้ `grid place-items-center` บน Outer Wrapper ที่เป็น `fixed inset-0`
  - Outer Wrapper ต้องมี `overflow-y-auto` เพื่อรองรับหน้าจอที่เล็กกว่าตัว Modal
- **โครงสร้าง Hierarchy (Body-Footer Separation):**
  - **กฎเหล็ก:** ต้องปิด container ส่วนที่เป็น "Body" (Scrollable) ก่อนที่จะเริ่มส่วน "Footer" (Sticky) เสมอ เพื่อป้องกันเนื้อหาไหลทับปุ่มบันทึก
  - Modal Root ต้องมี `max-h-[90vh]` และ `overflow-hidden` เพื่อคุมทรงหน้าต่าง
- **มาตรฐาน Timeline (Visual Timeline):**
  - เส้นแนวตั้งต้องใช้ `border-l-2` และ `pl-8` (หรือระยะที่เหมาะสม)
  - จุด Marker (Dots) ต้องใช้ `absolute -left-[XXpx]` ที่คำนวณมาอย่างดี เพื่อให้จุดอยู่กึ่งกลางเส้นพอดี (เช่น `-left-[41px]` สำหรับ `pl-8`)
  - ระยะห่างระหว่างหัวข้อใน Modal ควรใช้ `space-y-10` เพื่อความโปร่งสบาย (Premium Breathing Room)

## 15. มาตรฐานระบบแจ้งเตือน Hotspot Alert System (New Next.js Project)
มาตรฐานสำหรับการพัฒนาระบบแจ้งเตือนจุดความร้อนผ่านดาวเทียม (Satellite-based):

### Tech Stack & Libraries
- **Framework:** Next.js (App Router) + TypeScript
- **Mapping:** Leaflet (via `react-leaflet` or vanilla API wrapper)
  - **Critical:** ต้องใช้ `dynamic import` พร้อม `ssr: false` เสมอ เพื่อป้องกัน Hydration Error
- **Data Sources:** 
  - **Hotspots:** NASA FIRMS API (Key-based)
  - **Weather/Wind/PM2.5:** Open-Meteo API (Free, No-Key)
  - **Geography:** GeoJSON สำหรับอุทยานฯ/พื้นที่ป่าอนุรักษ์

### Performance Optimization Standards (Map Tuning)
- **Single Map Initialization:** ห้ามสร้าง `L.map` ใหม่ทุกครั้งที่ Render
  - ใช้ `useEffect` กับ `[]` (Empty deps) เพื่อสร้าง Map Instance ครั้งเดียว
  - ใช้ `useRef` เก็บ Instance (`mapInstanceRef`) และ Layer (`markersLayerRef`)
- **Efficient Updates:** เมื่อข้อมูลเปลี่ยน (`hotspots`) ให้ใช้วิธี `layer.clearLayers()` และ `layer.addLayer()` แทนการ Re-render ทั้ง Component
- **Memoization:** ใช้ `useMemo` สำหรับตัวแปรที่คำนวณหนักๆ หรือ Array/Object ที่ใช้เป็น Dependency (เช่น `mapCenter`, `center`)

### Progressive Web App (PWA) Standards
- **Manifest:** ต้องมี `public/manifest.json` ที่กำหนด `display: "standalone"` เพื่อให้ทำงานเหมือน Native App
- **Meta Tags:** ใน `layout.tsx` ต้องมี `appleWebApp` configuration (`capable: true`, `statusBarStyle`) เพื่อรองรับ iOS เต็มรูปแบบ
- **Icon Generation:** ต้องมีไอคอนชัดเจนขนาด 512x512 และ 192x192

### Environmental Monitoring Logic
- **Risk Calculation:** ใช้สูตรแบบง่าย (Heuristic) เพื่อแสดงผลทันที:
  - **Ultra High Risk:** Temp > 35°C AND Humidity < 30% AND Wind > 20 km/h
  - **High Risk:** Temp > 32°C AND Humidity < 40%
  - **Moderate:** ค่าอื่นๆ ปกติ
- **Data Fetching:** ควรดึงข้อมูล Weather และ Air Quality พร้อมกันเพื่อลด Round-trip และแสดงผลใน `EnvironmentCard` เดียว

### Timezone Handling Standards (Critical)
- **Problem:** Server (Cloud/Local) อาจมีการตั้งค่า Timezone ไม่เหมือนกัน (UTC+0 vs UTC+7). การบวกเวลาเอง (Manual Offset) เช่น `new Date(utc + 7 hours)` จะทำให้เกิดบั๊ก "Double Shift" หากรันบนเครื่องที่เป็นเวลาไทยอยู่แล้ว
- **Solution:** ห้ามบวกเวลาเองเด็ดขาด ให้ใช้ `Intl` API หรือ `toLocaleDateString` พร้อมระบุ Timezone เป้าหมายเสมอ
- **Code Pattern:**
  ```typescript
  // ✅ Correct Way:
  const thaiDate = dateObj.toLocaleDateString('en-CA', { timeZone: 'Asia/Bangkok' });
  
  // ❌ Incorrect Way (Do NOT use):
  const thaiTime = new Date(dateObj.getTime() + (7 * 60 * 60 * 1000));
  ```

---
*Skill นี้สร้างขึ้นเพื่อยกระดับการจัดการข้อมูลสู่ระบบอัจฉริยะ (AI-Powered Insights)*
